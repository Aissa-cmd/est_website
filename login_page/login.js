const login = document.getElementById('login');

login.addEventListener('submit', (e) => {
	console.log('the form submited');
	// this is where you can do some ajax
	// request to a backend to authenticate the user
	// to thier profile
})
